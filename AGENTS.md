# Repository guidelines

- Keep the site dependency-free unless a new requirement justifies a dependency.
- Preserve Brazilian Portuguese accents in user-facing content.
- Keep message templates in `app.js` and do not add real customer data or credentials.
- Run `npm run codex:check` after changes to site assets.
